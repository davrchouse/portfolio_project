$(document).ready(function(){
        $("div.description").hide();
 });


$(function() {
    var state = true;

    $("#resume_button").click(function() {
        if ( state ) {  
          $("#resume").show(100);
        } else {
          $("#resume").hide(100);
        }                             
        state = !state;
      });
    });


$(function() {
    var state = true;

    $("#map_button").click(function() {
        if ( state ) {  
          $("#map").show(100);
        } else {
          $("#map").hide(100);
        }                             
        state = !state;
      });
    });

$(function() {
    var state = true;

    $("#game_button").click(function() {
        if ( state ) {  
          $("#game").show(100);
        } else {
          $("#game").hide(100);
        }                             
        state = !state;
      });
    });


$(function() {
    var state = true;

    $("#feeder_button").click(function() {
        if ( state ) {  
          $("#feeder").show(100);
        } else {
          $("#feeder").hide(100);
        }                             
        state = !state;
      });
    });

$(function() {
    var state = true;

    $("#web_button").click(function() {
        if ( state ) {  
          $("#web").show(100);
        } else {
          $("#web").hide(100);
        }                             
        state = !state;
      });
    });

$(function() {
    var state = true;

    $("#heart_button").click(function() {
        if ( state ) {  
          $("#heart").show(100);
        } else {
          $("#heart").hide(100);
        }                             
        state = !state;
      });
    });


